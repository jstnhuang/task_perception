#include "ros/ros.h"
#include "task_db/demo_states_db.h"

namespace pbi {
// Expose an action to generate programs given a bag path
// Plan and retime is as needed
// Then execute it
ProgramGeneratorNode::ProgramGeneratorNode() {}
}  // namespace pbi

int main(int argc, char** argv) { return 0; }
